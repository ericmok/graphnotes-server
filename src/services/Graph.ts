import Auth from './Auth';

const Graph = {
  async createGraph(username: String, name?: String) {
    // Get the user?
    // Validate user
    // Valide user has permissions
    // Create graph for user
    // When creating graph, if no name, assign random name
  }
};

export default Graph;